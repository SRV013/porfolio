---
const { value, color } = Astro.props;
const bgColor = color === "sec" ? "bg-[#1e1b4b]" : "bg-[#3f3f46]";
---

<div class="flex items-center">
    <span class={`text-xs px-4 py-2 rounded-3xl ${bgColor}`}>
        {value}
    </span>
</div>
