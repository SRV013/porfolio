---
const { value, color  } = Astro.props;
const bgColor =
    color === "sec"
        ? "dark:bg-[#1e1b4b] bg-zinc-400"
        : "bg-indigo-900";
---

<div class="flex items-center">
    <span class={`text-sm px-3 py-1 rounded-3xl ${bgColor}`}>
        {value}
    </span>
</div>
